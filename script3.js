function multiplication(qty) {
    while () {
        for (i = 1; i <= 10; i++) {
            var ans = i * qty;
            document.write(i + " x " + qty + " = " + ans + "<br>");
        }
    }
}

multiplication(2)